<?xml version="1.0" ?>
<odoo>
    <data>
        <template id="management_user_form">
            <t t-call="portal.portal_layout">
                <t t-set="breadcrumbs_searchbar" t-value="False"/>

                <t t-set="group_categ" t-value="request.env['ir.module.category'].sudo().search([('name', '=', 'Business')])"/>
                <t t-set="user_groups" t-value="request.env['res.groups'].sudo().search([('category_id', '=', group_categ.id)])"/>
                <div id="user_form">
                    <h1 t-if="data.get('name')">Edit user</h1>
                    <h1 t-else="">Create user</h1>
                    <form id="createUserForm">
                        <input type="hidden" id="uid" name="uid" t-att-value="data.get('uid')"/>
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" class="form-control" id="name" name="name" t-att-value="data.get('name')"/>
                        </div>
                        <div class="form-group">
                            <label for="login">Email address</label>
                            <input type="email" class="form-control" id="login" name="login" t-att-value="data.get('login')"/>
                        </div>
                        <div class="form-group">
                            <label for="role">Role</label>
                            <select class="form-control" id="role" name="role">
                                <option>Select...</option>
                                <t t-foreach="user_groups" t-as="group">
                                    <option t-att-value="group.id" t-att-selected="group.id == data.get('role')">
                                        <t t-esc="group.name"/>
                                    </option>
                                </t>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="password">Password</label>
                            <input type="password" class="form-control" id="password" name="password" t-att-value="data.get('password')" t-att-readonly="data.get('password')"/>
                        </div>
                        <button type="submit" class="btn btn-primary">Save</button>
                    </form>
                </div>

                <div class="modal" id="confirmModal" tabindex="-1" role="dialog" aria-labelledby="confirmModal" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span>&#xD7;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <p t-if="data.get('name')">User created successfully</p>
                                <p t-else="">User updated successfully</p>
                            </div>
                            <div class="modal-footer">
                                <a class="btn btn-primary" data-dismiss="modal" onclick="window.location.href = '/my/business/users'">Accept</a>
                            </div>
                        </div>
                    </div>
                </div>
            </t>
        </template>
    </data>
</odoo>